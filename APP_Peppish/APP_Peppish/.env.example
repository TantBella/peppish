# Example environment variables for APP_Peppish
# Point the frontend to the deployed API
VITE_API_URL=https://your-api-url.com

# If you want to force seeding local dev data even when API URL is set,
# set VITE_USE_SEED=true (NOT recommended against a production API)
VITE_USE_SEED=false
